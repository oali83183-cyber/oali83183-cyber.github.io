# 🚀 دليل رفع الموقع على GitHub Pages (موقع دائم ومجاني)

> خطوات من **الكمبيوتر** — اتبعها بالترتيب ولا تتخطى خطوة.

---

## المرحلة 1 — إنشاء حساب GitHub (مرة واحدة فقط)

1. افتح المتصفح (كروم / إيدج / بريف) وادخل إلى: **github.com**
2. اضغط زر **"Sign up"** (أخضر، أعلى اليمين)
3. املأ:
   - **Email**: بريدك الإلكتروني
   - **Password**: كلمة مرور (8 أحرف على الأقل)
   - **Username**: اسم مستخدم — **اكتبه بحروف إنجليزية** مثل: `ali-pivot`
   - اضغط **Continue**
4. أكمل فحص الأمان (لغز صغير) واضغط **Create account**
5. افتح بريدك → اضغط على رابط **التحقق** الذي أرسله GitHub
6. ✅ **تم إنشاء حسابك**

---

## المرحلة 2 — إنشاء المستودع (Repository)

1. من الصفحة الرئيسية اضغط **زر "+"** (أعلى اليمين) → **"New repository"**
2. في خانة **Repository name** اكتب: `username.github.io`
   - ⚠️ **مهم جداً:** استبدل `username` باسم المستخدم الذي سجلت به بالضبط
   - مثال: إذا اسمك `ali-pivot` → تكتب `ali-pivot.github.io`
3. اختر **Public** (عام — مجاني)
4. اترك باقي الخيارات كما هي
5. اضغط **"Create repository"** (أخضر أسفل الصفحة)

---

## المرحلة 3 — رفع ملف الموقع

1. داخل المستودع الجديد، اضغط **"uploading an existing file"** (رابط صغير)
2. افتح مجلد **"موقعي_اونلاين"** على جهازك (الذي نزّلته من العارض)
3. **اسحب ملف `index.html`** إلى منطقة السحب، أو اضغط "choose your files" واختره
4. تحت الملف، اكتب وصفاً صغيراً (اختياري) مثل: "برنامج علي التدريبي"
5. اضغط زر **"Commit changes"** (أخضر)
6. ✅ **تم رفع الملف**

---

## المرحلة 4 — تفعيل GitHub Pages

1. داخل المستودع، اضغط على تبويب **"Settings"** (إعدادات — أعلى اليمين)
2. من القائمة الجانبية اليسرى، اضغط **"Pages"**
3. في قسم **"Build and deployment"**:
   - **Source**: اختر "Deploy from a branch"
   - **Branch**: اختر `main` → بجانبها `/ (root)` → اضغط **Save**
4. انتظر **1–3 دقائق** (أحياناً يظهر أصفر "درجة التحميل")

---

## المرحلة 5 — موقعك جاهز! 🎉

1. بعد دقائق، افتح المتصفح واكتب:
   ```
   https://username.github.io
   ```
   (بنفس اسم المستخدم — مثلاً: `https://ali-pivot.github.io`)

2. **سيعمل برنامجك أونلاين!** ✅

3. احفظ الرابط في ملاحظات هاتفك — من الآن تفتحه من أي جهاز (كمبيوتر، هاتف، تابلت)

---

## 📱 تثبيته على هاتفك (بعد أن يصبح أونلاين)

1. افتح رابط موقعك في **بريف** على الهاتف
2. اضغط **⋮** → **"إضافة إلى الشاشة الرئيسية"**
3. ستجد أيقونة 🏐 على شاشتك → تضغطها ويفتح مباشرة

---

## ⚠️ تنبيه مهم — بياناتك!

برنامجك يحفظ تسجيلاتك (النوم، الطاقة، الوزن، الاستلام...) في **المتصفح** الذي تستخدمه:
- إذا فتحت الموقع من **الكمبيوتر** → بياناتك على الكمبيوتر
- إذا فتحته من **الهاتف** → بياناتك على الهاتف
- **البيانات لا تنتقل تلقائياً بين الأجهزة**

**لذلك:** أرسل لي تقريرك كل يوم (زر "📤 إرسال للمدرب") — أحفظه عندي في ملفاتي، فتبقى نسختك محفوظة دائماً بغض النظر عن الجهاز.

---

## ❓ لو علقت — الحلول السريعة

| المشكلة | الحل |
|---|---|
| الرابط لا يفتح | تأكد أنك كتبت اسم المستخدم بالضبط مثل الحساب، وانتظر دقيقة أخرى |
| الصفحة بيضاء | تأكد أن اسم الملف هو `index.html` بالضبط (وليس `index.html.txt`) |
| ما ألقى تبويب Pages | تأكد أنك داخل Settings للمستودع (وليس الحساب) |
| رفع الملف فشل | أعد المحاولة، أو سجّل الدخول من جديد |

**وإذا واجهت أي شيء غريب — صوّر الشاشة وأرسلها لي، وأنا أحل معك 🤝**
